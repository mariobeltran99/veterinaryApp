import { FilterAnimalsPipe } from './filter-animals.pipe';

describe('FilterAnimalsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAnimalsPipe();
    expect(pipe).toBeTruthy();
  });
});
