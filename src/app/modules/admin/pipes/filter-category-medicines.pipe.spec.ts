import { FilterCategoryMedicinesPipe } from './filter-category-medicines.pipe';

describe('FilterCategoryMedicinesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCategoryMedicinesPipe();
    expect(pipe).toBeTruthy();
  });
});
