import { FilterCategoryProductsPipe } from './filter-category-products.pipe';

describe('FilterCategoryProductsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCategoryProductsPipe();
    expect(pipe).toBeTruthy();
  });
});
