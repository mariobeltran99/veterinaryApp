import { FilterDepartmentsPipe } from './filter-departments.pipe';

describe('FilterDepartmentsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDepartmentsPipe();
    expect(pipe).toBeTruthy();
  });
});
