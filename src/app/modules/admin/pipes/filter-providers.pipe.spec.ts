import { FilterProvidersPipe } from './filter-providers.pipe';

describe('FilterProvidersPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProvidersPipe();
    expect(pipe).toBeTruthy();
  });
});
