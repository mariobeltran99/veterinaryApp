import { FilterUsersClientsPipe } from './filter-users-clients.pipe';

describe('FilterUsersClientsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterUsersClientsPipe();
    expect(pipe).toBeTruthy();
  });
});
